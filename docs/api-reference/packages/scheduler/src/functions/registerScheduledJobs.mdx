# Function: registerScheduledJobs()

```ts
function registerScheduledJobs(instance): TaskHandle[];
```

Defined in: [packages/scheduler/src/utils/scheduler.utils.ts:42](https://github.com/kiyasov/elysia-nest/blob/main/packages/scheduler/src/utils/scheduler.utils.ts#L42)

Scans `instance` for methods decorated with [Cron](Cron.mdx), [Interval](Interval.mdx),
[Timeout](Timeout.mdx), or [ScheduleAt](ScheduleAt.mdx) and registers them with the global
[Scheduler](../classes/Scheduler.mdx).

Called automatically by the DI container after a provider is instantiated.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `instance` | `unknown` |

## Returns

[`TaskHandle`](../interfaces/TaskHandle.mdx)[]

The [TaskHandle](../interfaces/TaskHandle.mdx) array for every successfully registered job.
